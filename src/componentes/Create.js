//vsariables donde se creara cada cardbx
const frontContainer = document.querySelector(".cards_container_front");
const backContainer = document.querySelector(".cards_container_back");
const innContainer = document.querySelector(".cards_container_inn");

function createCard (titulo, categoria, imagen, video, descripcion){

}

//obtener los valores del formulario para la funcion
form.addEventListener("submit", (event) => {
    event.preventDefault();

    const titulo = document.querySelector("[data-titulo]").value;
    const categoria = document.querySelector("[data-categoria]").value;
    const imagen = document.querySelector("[data-imagen]").value;
    const video = document.querySelector("[data-video]").value;
    const descripcion = document.querySelector("[data-descripcion]").value;
})